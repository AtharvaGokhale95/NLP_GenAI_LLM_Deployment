# ANN Implementation End To End

Using the CHURN MODELLING dataset: The output of the model is to predict if the Customer will leave the Bank - Y/N
1. We will perform feature engineering 
2. Use Drop Out: To avoid over-fitting
3. Convert the model into a file format to deploy (pickle, h5) -> Deploy in Streamlit cloud
4. 